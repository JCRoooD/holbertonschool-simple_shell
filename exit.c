#include <stdio.h>

int main() {
    // No se imprime nada en la salida estándar (stdout)
    
    // No se imprime nada en la salida de error estándar (stderr)
    
    // Retorna un valor de estado 0
    return 0;
}
